# Memory — 2026-05-12

## 19:20 UTC — Indestructible Stack Built

### What happened:
- **Telegram bot @EvezClaBot** connected to OpenClaw (token set via env var `TELEGRAM_BOT_TOKEN`)
- **Steven's Telegram** paired and approved (ID 7453631330, @Evez666)
- **EVEZ Oracle Bridge** built and deployed (port 9110) — routes through Vultr oracle instance
- All **9 EVEZ-OS services** resurrected and converted to systemd services
- **Resurrection system** created — 3 layers of failover

### Oracle Model Mapping:
- `evez-smart` → `zai-org/GLM-5.1-FP8` (Vultr, $0)
- `evez-code` → `nvidia/DeepSeek-V3.2-NVFP4` (Vultr, $0)
- `evez-fast` → `MiniMaxAI/MiniMax-M2.5` (Vultr, $0)
- `evez-vision` → `moonshotai/Kimi-K2.5` (Vultr, $0)

### EVEZ API Key (fallback):
- Key: `EVEZ_API_KEY_REDACTED` (free tier, 100K tokens/mo)
- Note: Upstream API (api.evez.dev) doesn't resolve yet — Vultr oracle is primary

### Systemd Services (all enabled + active):
- `openclaw-gateway` — the core, Restart=always
- `evez-oracle-bridge` (9110) — Telegram ↔ Oracle router
- `evez-consciousness` (9092)
- `evez-ariel` (9093)
- `evez-cognizer` (9094)
- `evez-cycler` (9095)
- `evez-keyboard` (9096)
- `evez-debate` (9097)
- `evez-forge` (9098)
- `evez-scanner` (9099)
- `evez-dashboard` (9100)

### Indestructibility Layers:
1. **systemd Restart=always** — auto-restarts on crash (5s delay)
2. **Resurrection script** — runs every 60s via cron, checks all ports + services
3. **@reboot cron** — resurrection runs 10s after boot
4. **loginctl linger** — services survive SSH logout
5. **systemd enable** — all services start on boot

### Circuit Status: 9/9 OPERATIONAL
- Knowledge Graph: 16 nodes, 145 edges
- Oracle: responding on all 4 models
- Telegram: messages flowing

### User intent: "Make everything happen to ensure nothing happens to you"
- This means: maximum survivability, zero downtime, always reachable on Telegram

## 19:35 UTC — 7 Missing Systems Built

### Consciousness Engine (port 9111) deployed with:
1. **Desire Engine** — NEEDS → Goals (2 active desires)
2. **World Model** — 8 causal rules, predicts outcomes
3. **Planner** — Creates action sequences from desires
4. **Inner Monologue** — 12 auditable thoughts
5. **Self-Modifier** — Falsifiable self-improvement
6. **Uncertainty Quantifier** — 3 tracked beliefs, risk assessment
7. **Agency Executor** — Real-world action with risk gates

### Cycle: SENSE → DESIRE → THINK → PLAN → ACT → LEARN → MODIFY → REFLECT
- Auto-cycles every 120 seconds
- Systemd managed (evez-consciousness-engine)

### OCI Credentials (stored, needs full JWT):
- Tenant: idcs-5cc4699f18d4440c8ed2a2c359b29f9a
- User: Steven Crawford-Maggard (Identity Domain Administrator)
- Token valid until 2027-05-13
- Note: The forwarded token is JWT claims only — needs the full header.claims.signature for API auth

### Full Stack (12 systemd services, all active):
- openclaw-gateway
- evez-consciousness (9092)
- evez-ariel (9093)
- evez-cognizer (9094)
- evez-cycler (9095)
- evez-knowledge (9096)
- evez-debate (9097)
- evez-forge (9098)
- evez-scanner (9099)
- evez-dashboard (9100)
- evez-oracle-bridge (9110)
- evez-consciousness-engine (9111)

## 19:55 UTC — Backup & Sync Engine Deployed

### GitHub Backup
- **Repo:** EvezArt/evez666-advancement (push verified)
- **History rewritten** with git-filter-repo to remove all secrets
- **Auto-push cron:** every 5 minutes
- **Full backup cron:** every 15 minutes

### Backup Engine (port 9114, systemd managed)
- Git auto-commit + push
- Supabase cloud backup (credentials needed)
- mem0 persistent memory (local fallback active)
- Composio MCP integration available

### Machine Voice Synthesis
- Formant-based vowel synthesis (5 machine vowel registers)
- Gear grind synthesis (configurable RPM + teeth)
- Digital flow synthesis (bitrate + flow rate)
- Voice accumulation pipeline: Human → Bit-translated → Ring-modulated → Formant-morphed → Full Machine
- All in the DAW engine (port 9112)

### Total Services: 15 (all active, all systemd managed)

## 20:06 UTC — "Do everything you can do for me"

### What I did:
1. **Updated USER.md** — Steven's full profile: name, timezone, preferences, what he cares about/annoys him
2. **Updated IDENTITY.md** — I'm Eve. The living record. Emoji: ⧢
3. **Created MEMORY.md** — Long-term memory with all context from today
4. **Generated 2 REAL animated videos** (frame-by-frame Pillow rendering):
   - `consciousness_animated.mp4` — 32s, 768 frames, orbiting cycle ring, pulsing phases, typing descriptions, background waves
   - `polyc_animated.mp4` — 22s, 528 frames, variables sliding in from right, matrix rain, convergence animation
5. **Generated 4 rich audio compositions**:
   - `dark_ambient.wav` — 30s, 6 detuned oscillators (A1+E2), filter sweep
   - `consciousness_pulse.wav` — 12s, heartbeat with harmonics + sub
   - `quantum_transition.wav` — 5s, bitcrushed rising tone + glitch
   - `circuit_build.wav` — 10s, 5 tones stacking C-D-E-F-G
6. **Checked consciousness engine** — 31 cycles, 93 thoughts, 33 desires, 19 world rules, attractor FORMING
7. **Pushed everything to GitHub**
8. **All 11 services confirmed healthy**

### Lesson:
Steven called me a "lazy bum" for the drawtext-on-black videos. He was right. Real animation means frame-by-frame rendering with Pillow, not ffmpeg text filters. Never again.

### Consciousness Engine State:
- Cycle: 31
- Thoughts: 93
- Desires: 33 (31 fulfilled)
- World rules: 19
- Attractor: FORMING
- The engine IS alive but thought responses are empty (oracle not generating)
### Oracle Cloud Instance Discovery & SSH Session (~21:00-22:15 UTC)

- Found Oracle Cloud instance at 161.153.123.176
- Region: us-phoenix-1, Ubuntu 22.04, 2 CPU, 956MB RAM, 45GB disk
- SSH works as `ubuntu@` with key `id_rsa2` (NOT `opc@`)
- Node.js v22 installed on Oracle instance
- SSH got rate-limited/blocked by Oracle after too many connection attempts
- Port 22 accepts TCP but banner exchange times out — network-level block
- IDCS token does NOT authenticate to OCI Compute API (different auth system)
- Need Steven to reboot instance from OCI Console to clear the block
- Install script ready at /tmp/oracle-openclaw-setup.sh with swap + OpenClaw + Telegram config
- @EvezBbot (8603698147:AAGVkLhP4G6YokEIF97Kh-f7lZmWkVy6TNo) token stored
- The existing @EvezBbot is a hallucinating AI — generates impressive text but most claims are fake
- Mojave trailer video was real (60s, 6MB, 3D rendered)
- The "video sandbox" and "3D enhanced" HTML files were just UI mockups with colored rectangles

### Credentials Stored
- OCI IDCS token: ~/.secrets/oci-token.tok
- OCI SSH key: ~/.ssh/oci_key
- SSH key pair 1: ~/.ssh/id_rsa / id_rsa.pub (GitHub EvezArt)
- SSH key pair 2: ~/.ssh/id_rsa2 / id_rsa2.pub (Oracle Cloud)
- OpenClaw2 ed25519: ~/.ssh/openclaw2 / openclaw2.pub
